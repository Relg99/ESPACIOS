<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.espacios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuStoreActivity"></activity>
        <activity android:name=".StoreInfoActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".OptimizationActivity" />
        <activity android:name=".RegisterAccountActivity" />
        <activity android:name=".EmailCodeActivity" />
        <activity android:name=".RegisterStoreActivity" />
        <activity android:name=".AccountOptionsActivity" />
        <activity android:name=".CounterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".ClientActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>